/*Escribe una función que recibe como parámetros dos tablas de estructuras como
las del ejercicio anterior, un entero con su longitud (que es la misma para ambas),
y un float. La función procesa las dos tablas a la vez (primero con primero,
segundo con segundo, etc.) y devuelve el índice del primer par de elementos que
están cerca tal y como se calcula con la función del apartado anterior utilizando el
último parámetro como distancia. Si la condición no se cumple para ningún par de
elementos, devuelve el valor -1.*/

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
   
   
}


